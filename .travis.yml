language: crystal
crystal:
  - latest
script:
  - crystal spec
  - cd ./examples && for f in `ls -1 ./*.cr` ; do (crystal run $f &> /dev/null && print ".") || print "F" ; done && cd ..
  - cd ./examples && for f in `ls -1 ./*.cr` ; do (crystal run $f &> /dev/null ; done && cd ..
